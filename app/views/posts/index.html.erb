<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <h1>投稿一覧</h1>
    <% @posts.each do |post| %>
      <%= render partial: "post", locals: { post: post } %>
    <% end %>
    <%= paginate(@posts) %>
  </div>
</div>
