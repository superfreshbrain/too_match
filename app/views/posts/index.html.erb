<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <h1>投稿一覧</h1>
    <% @posts.each do |post| %>
      <%= render partial: "post", locals: { post: post } %>
      <%#= render partial: "post", collection: @posts %>
    <% end %>
    <%= paginate(@posts) %>
  </div>
</div>
