<div class="container">
  <div class="row">
  <p><%= @username %>さんの投稿一覧</p>
  <% @posts.each do |post| %>
    <div class="content_post" style="background-image: url(<%= post.image %>);">
      <%= simple_format(post.description) %>
    </div>
  <% end %>
  <%= paginate(@posts) %>
  </div>
</div>
