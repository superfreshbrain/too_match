<div class="container">
  <div class="row">
  <p><%= @username %>さんの投稿一覧</p>
  <% @posts.each do |post| %>
    <%= render partial: "posts/post", locals: { post: post } %>
  <% end %>
  <%= paginate(@posts) %>
  </div>
</div>
